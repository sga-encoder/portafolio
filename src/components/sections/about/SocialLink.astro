---
import type { SocialLink } from "../../../data/profile";

interface Props {
  link: SocialLink;
}

const { link } = Astro.props;
const isExternal = link.platform !== "email";
---

<a
  href={link.href}
  class="flex items-center gap-3 text-ink transition-colors hover:text-brand"
  target={isExternal ? "_blank" : undefined}
  rel={isExternal ? "noreferrer" : undefined}
>
  <span
    class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg border-2 border-current"
    aria-hidden="true"
  >
    {
      link.platform === "instagram" && (
        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="1.8">
          <rect x="3" y="3" width="18" height="18" rx="5" />
          <circle cx="12" cy="12" r="4.2" />
          <circle cx="17.2" cy="6.8" r="1.1" fill="currentColor" stroke="none" />
        </svg>
      )
    }
    {
      link.platform === "linkedin" && (
        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="1.8">
          <rect x="3" y="3" width="18" height="18" rx="3" />
          <circle cx="8" cy="9" r="0.9" fill="currentColor" stroke="none" />
          <line x1="8" y1="12" x2="8" y2="17" />
          <line x1="12.5" y1="17" x2="12.5" y2="12.5" />
          <path d="M12.5 14.2c0 -1.5 3.5 -1.5 3.5 0v2.8" />
        </svg>
      )
    }
    {
      link.platform === "email" && (
        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="1.8">
          <rect x="3" y="5" width="18" height="14" rx="2.5" />
          <path d="M4 7l8 6 8-6" />
        </svg>
      )
    }
  </span>
  <span class="font-body text-sm sm:text-base">{link.label}</span>
</a>
