/**
 * Colores fijos del fondo/tarjetas de `/proyectos` (listado): a diferencia de
 * `/proyectos/[slug]`, aquí no hay una única portada de la que extraer 2 colores
 * dominantes — se usan los mismos 2 acentos de marca que `dominantColors.ts` ya
 * usa como fallback (`--color-accent`/`--color-accent-2`).
 */
export const TIMELINE_COLORS: [string, string] = ["#C0009D", "#0033FF"];
