import carouselJson from "./carousel.json";

/** Ids de proyecto, en orden, que aparecen en el carrusel de Proyectos de Inicio (071). */
export const CAROUSEL_PROJECT_IDS: string[] = carouselJson as string[];
