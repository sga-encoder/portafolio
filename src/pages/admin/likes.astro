---
import BaseLayout from "../../layouts/BaseLayout.astro";
import LikesPanel from "../../components/admin/LikesPanel";
import { buildProjectCards } from "../../lib/admin/projectCards";

const projects = await buildProjectCards();
---

<BaseLayout title="Admin · Estadísticas" noindex>
  <LikesPanel client:load projects={projects} />
</BaseLayout>
